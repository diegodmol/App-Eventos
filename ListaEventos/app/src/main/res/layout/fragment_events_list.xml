<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_drawable">


    <ImageView
        android:id="@+id/itemImage"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:layout_margin="@dimen/margin_small"
        app:layout_constraintDimensionRatio="4:2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_image_event" />

    <TextView
        android:id="@+id/txtInformation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_small"
        android:gravity="center"
        android:maxLines="2"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/itemImage"
        tools:text="Uma simples e perfeitamente extraída dose de espresso misturado com leite fresco e chocolate fazem esta bebida deliciosa." />


    <ImageView
        android:id="@+id/imgShare"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:layout_margin="@dimen/margin_small"
        app:layout_constraintTop_toBottomOf="@id/txtInformation"
        app:layout_constraintEnd_toEndOf="@id/txtInformation"
        android:src="@drawable/ic_share" />

    <ImageView
        android:id="@+id/imgFavorite"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:layout_margin="@dimen/margin_small"
        app:layout_constraintTop_toBottomOf="@id/txtInformation"
        app:layout_constraintEnd_toStartOf="@id/imgShare"
        android:src="@drawable/ic_favorite" />

    <ImageView
        android:id="@+id/imgFavoriteLike"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:layout_margin="@dimen/margin_small"
        app:layout_constraintTop_toBottomOf="@id/txtInformation"
        app:layout_constraintEnd_toStartOf="@id/imgShare"
        android:visibility="gone"
        android:src="@drawable/ic_favorite_like" />


</androidx.constraintlayout.widget.ConstraintLayout>